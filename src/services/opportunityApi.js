import {
  getOpportunities,
  getOpportunityById,
  checkEligibility,
  compareOpportunities,
} from '../mocks/api';

export const opportunityApi = {
  getOpportunities,
  getOpportunityById,
  checkEligibility,
  compareOpportunities,
};